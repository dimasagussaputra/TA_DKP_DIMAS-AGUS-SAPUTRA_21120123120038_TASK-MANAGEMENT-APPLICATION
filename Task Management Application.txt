import tkinter as tk

class Task:
    def __init__(self, name, task_given_date, deadline_date):
        self.name = name
        self.task_given_date = task_given_date
        self.deadline_date = deadline_date
        self.completed = False

    def mark_as_completed(self):
        self.completed = True

class TaskManager:
    def __init__(self):
        self.tasks = []

    def add_task(self, task):
        self.tasks.append(task)

    def display_tasks(self):
        for index, task in enumerate(self.tasks, start=1):
            task_text = f"{task.name} - Tanggal Pemberian Tugas: {task.task_given_date} - Deadline Tugas: {task.deadline_date} - Selesai: {'Ya' if task.completed else 'Belum'}"
            if not task.completed:
                task_text += f" - [Tandai Selesai]"
            tasks_display.insert(tk.END, f"{index}. {task_text}\n")

    def mark_task_as_completed(self, index):
        if 0 <= index < len(self.tasks):
            self.tasks[index].mark_as_completed()
            tasks_display.delete(1.0, tk.END)
            self.display_tasks()

def add_task():
    name = name_entry.get()
    task_given_date = task_given_date_entry.get()
    deadline_date = deadline_date_entry.get()
    new_task = Task(name, task_given_date, deadline_date)
    task_manager.add_task(new_task)

    name_entry.delete(0, tk.END)
    task_given_date_entry.delete(0, tk.END)
    deadline_date_entry.delete(0, tk.END)

    name_entry.focus_set()

    tasks_display.delete(1.0, tk.END)
    task_manager.display_tasks()

def mark_task(event):
    index = int(tasks_display.index(tk.CURRENT).split(".")[0]) - 1
    task_manager.mark_task_as_completed(index)

task_manager = TaskManager()

root = tk.Tk()
root.title("Task Management Application")

name_label = tk.Label(root, text="Nama Tugas:")
name_label.grid(row=0, column=0)
name_entry = tk.Entry(root)
name_entry.grid(row=0, column=1)

task_given_date_label = tk.Label(root, text="Tanggal Pemberian Tugas:")
task_given_date_label.grid(row=1, column=0)
task_given_date_entry = tk.Entry(root)
task_given_date_entry.grid(row=1, column=1)

deadline_date_label = tk.Label(root, text="Deadline Tugas:")
deadline_date_label.grid(row=2, column=0)
deadline_date_entry = tk.Entry(root)
deadline_date_entry.grid(row=2, column=1)

add_button = tk.Button(root, text="Tambah Tugas", command=add_task)
add_button.grid(row=3, column=0, columnspan=2)

tasks_display = tk.Text(root)
tasks_display.grid(row=4, column=0, columnspan=2)
tasks_display.config(state=tk.NORMAL)

tasks_display.bind("<Button-1>", mark_task)

root.mainloop()
